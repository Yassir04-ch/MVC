RewriteEngine On

# accept loading of acyual -f files and -d directories
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# sebd everything else to the index page
RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]